# Aider Prompt — list-sw-dev-file-trees-en

This is the exact `--message` text passed to aider by the GitHub Actions workflow.
Edit here to tune what the LLM does each bi-weekly run.

---

```
Follow the instructions in skills/list-sw-dev-file-trees-en/SKILL.md exactly.

Today's date is {TODAY}.

REWRITE README.md completely from scratch — do not patch or append to the
existing content. Replace the entire file with exactly this structure:

# 25 Most Used File Trees in Software Development

> Ordered by prevalence of use across the software development ecosystem.
> Last updated: {TODAY}

| # | Name | Summary Description | Detail |
|---|------|---------------------|--------|
| 1 | ... | ... | ... |
...
| 25 | ... | ... | ... |

---

*Generated by [list-sw-dev-file-trees-en](skills/list-sw-dev-file-trees-en/SKILL.md)
skill — runs bi-weekly via GitHub Actions.*

Rules:
- Exactly 25 rows, numbered 1–25, re-ranked if ecosystem adoption has shifted
- Name column: bold
- Summary Description: ≤ 12 words
- Detail: exactly 2 sentences

Do not modify any other file.
```

---

The workflow substitutes `{TODAY}` with the actual date via `sed` before passing
the message to aider. CHANGELOG.md is updated separately by the workflow shell.
See `.github/workflows/update-trees.yml`.
